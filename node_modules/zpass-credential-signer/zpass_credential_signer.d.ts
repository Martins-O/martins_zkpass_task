/* tslint:disable */
/* eslint-disable */
export function hash_to_fields_size_8(inputs: string[], network: Network): string[];
export function sign_merkle_root(private_key: string, root: string, network: Network): string;
export function get_merkle_proof(inputs: string[], index: number, network: Network): string[];
export function get_merkle_root(inputs: string[], network: Network): string;
export function get_merkle_tree(inputs: string[], network: Network): any;
/**
 * Exposes a Rust function to JavaScript for signing messages.
 * Returns the response as `SignResponse` or a `JsValue` error.
 */
export function sign_message(private_key: string, message: SignInboundMessage, hash_alg: HashAlgorithm, network: Network): SignResponse;
/**
 * Exposes a Rust function to JavaScript for converting a string option to a field value.
 */
export function get_field_from_value(str: string | null | undefined, network: Network): string;
export function verify_signed_credential(signature: string, address: string, message: string, network: Network): boolean;
/**
 * An enum representing the various hash algorithms supported.
 */
export enum HashAlgorithm {
  POSEIDON2 = 0,
  BHP1024 = 1,
  SHA3_256 = 2,
  KECCAK256 = 3,
}
export enum Network {
  Testnet = 0,
  Mainnet = 1,
}
/**
 * A struct representing the message to be signed in.
 */
export class SignInboundMessage {
  free(): void;
  /**
   * Constructor for `SignInboundMessage`.
   */
  constructor(data: any);
  readonly data: any;
}
/**
 * A struct representing the response of a signing operation.
 */
export class SignResponse {
  private constructor();
  free(): void;
  /**
   * Returns the signature from the response.
   */
  signature: string;
  /**
   * Returns the hash from the response.
   */
  hash: string;
}
